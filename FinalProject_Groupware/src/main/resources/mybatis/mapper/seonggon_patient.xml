<?xml version="1.0" encoding="UTF-8"?>

<!-- ==== mapper 기본설정 ==== -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- ==== 루트 엘리먼트 & 네임스페이스 설정(프로젝트 전체내에서 유일해야 한다.) ==== -->
<mapper namespace="patient">
	<!-- 
	<select id="selectList" resultType="TreatPatientVO">
		select P.patient_name, P.patient_gender, P.patient_jubun, P.patient_symptom, D.child_dept_name
		from tbl_patient P LEFT JOIN tbl_child_dept D
		on P.fk_child_dept_no = D.child_dept_no
		order by P.patient_no desc
	</select>
	 -->	
</mapper>